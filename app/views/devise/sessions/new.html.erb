<div class="row vh-100">
  
  <!-- The left half -->
  <div class="bg-image col-md-7 d-none d-lg-block">
    <div class="d-flex vh-100 align-items-center">
      <div class="col-12 text-center mx-auto text-white">
        <p class="h1 header-font">Devium</p>
        <h5 class="lead pb-2">Social Networking for Software Developers</h5>
        <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
          <%= link_to "Sign up", new_registration_path(resource_name), class: 'btn btn-primary rounded-pill text-uppercase text-font font-weight-bold' %>
        <% end %>
      </div>
    </div>
  </div><!-- end -->


  <!-- The right half -->
  <div class="col-md-5 my-auto">
    <div class="login shadow rounded py-5 mx-4 bg-light">
      <div class="container text-center">
        <div class="row">
          <div class="col-lg-10 mx-auto">
            <p class="h1 header-font">Devium</p>
            <h2 class="h3 header-font">Welcome back!</h2>
 
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <div class="form-group mb-3">
                <br>
                <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'Email', class: 'form-control form-control-lg rounded-pill border-0 shadow-sm px-4' %>
              </div>

              <div class="form-group mb-3">
                <br>
                <%= f.password_field :password, autocomplete: "current-password", placeholder: 'Password', class: 'form-control form-control-lg rounded-pill border-0 shadow-sm px-4' %>
              </div>

              <% if devise_mapping.rememberable? %>
                <div class="custom-control custom-checkbox mb-3">
                  <%= f.check_box :remember_me, class: 'custom-control-input', checked: true %>
                  <%= f.label :remember_password, class: 'custom-control-label' %>
                </div>
              <% end %>

              <div class="actions">
                <%= f.submit "Login", class: 'btn btn-primary text-uppercase mb-2 rounded-pill shadow-sm form-control form-control-lg font-weight-bold' %>
              </div>
            <% end %>

            <%= render "devise/shared/links" %>
          </div>
        </div>
      </div>
    </div>
  </div><!-- end -->
</div>